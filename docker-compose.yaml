services:
  clickhouse:
    image: clickhouse/clickhouse-server:24.1-alpine
    container_name: signoz-clickhouse
    volumes:
      - ./clickhouse-config.xml:/etc/clickhouse-server/config.d/memory.xml
      - ./data/clickhouse:/var/lib/clickhouse
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 30s
      timeout: 10s
      retries: 10
    restart: on-failure

  signoz:
    image: signoz/signoz:v0.122.0
    container_name: signoz-unified
    environment:
      - ClickHouseUrl=tcp://clickhouse:9000
      - SIGNOZ_LOCAL_DB_PATH=/var/lib/signoz/signoz.db
    volumes:
      - ./data/signoz:/var/lib/signoz
    depends_on:
      - clickhouse
    deploy:
      resources:
        limits:
          memory: 450M
    restart: on-failure

  otel-collector:
    image: signoz/signoz-otel-collector:v0.144.3
    container_name: signoz-otel-collector
    command: ["--config=/etc/otel-collector-config.yaml"]
    volumes:
      - ./otel-collector-config.yaml:/etc/otel-collector-config.yaml
    environment:
      - OTEL_RESOURCE_ATTRIBUTES=host.name=signoz-host,os.type=linux
    ports:
      - "4317:4317"
      - "4318:4318"
    depends_on:
      - clickhouse
    deploy:
      resources:
        limits:
          memory: 150M
    restart: on-failure

  caddy:
    image: caddy:2-alpine
    container_name: signoz-proxy
    ports:
      - "80:80"
      - "443:443"
    environment:
      - SIGNOZ_DOMAIN=${SIGNOZ_DOMAIN}
      - SIGNOZ_ADMIN_PASSWORD_HASH=${SIGNOZ_ADMIN_PASSWORD_HASH}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./data/caddy_data:/data
      - ./data/caddy_config:/config
    depends_on:
      - signoz
    deploy:
      resources:
        limits:
          memory: 80M
    restart: unless-stopped

networks:
  default:
    name: signoz-network
